apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  annotations:
    sail.ingress.kubernetes.io/middlewares: |
      - jwt
spec:
  ingressClassName: sail
  rules:
  - host: zero.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: backend-sail
            port:
              number: 8097
